# Περιβάλλον Python (Ποτέ δεν ανεβάζουμε το venv γιατί είναι τεράστιο)
venv/
env/

# Προσωρινά αρχεία Python
__pycache__/
*.pyc
*.pyo

# ΚΡΥΦΑ ΚΛΕΙΔΙΑ (ΤΟ ΠΙΟ ΣΗΜΑΝΤΙΚΟ)
.env

# Μνήμη Jarvis (Για να μην ανέβουν τα προσωπικά σου δεδομένα στο ίντερνετ)
profile.json

# Προσωρινά αρχεία ήχου
temp_response.mp3